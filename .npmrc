# All used peers are explicit dependencies. The only peer-only subtree in the
# lockfile is electron-builder's unused Squirrel.Windows target (temp/rimraf 2).
# Keep resolving peers in the lockfile, but do not install that unused target.
omit=peer
